export default function UserButton() {
  return (
    <button
      style={{
        padding: "8px 12px",
        background: "#007bff",
        color: "white",
        borderRadius: 6,
      }}
    >
      👤 Remote1 - Users
    </button>
  );
}
